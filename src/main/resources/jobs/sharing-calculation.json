{
  "id": "sharing-calculation",
  "name": "Sharing Calculation A",
  "version": "1.0",
  "description": "Recalculate sharing for Account records using the SF Apex REST endpoint",
  "query": "SELECT Id FROM Account ORDER BY Id ASC",
  "plugins": ["LogPlugin"],
  "defaultBatchSize": 1000,
  "defaultThreads": 5
}
